[workspace]
members = [
    "welding",
    "demo-weld-win",
    "demo-weld-linux",
    "demo-weld-mac",
]
default-members = ["welding"]
resolver = "3"

[workspace.package]
edition = "2024"
license = "MPL-2.0"
repository = "https://github.com/merely-made/wgpu-weld"
version = "0.1.0"

# Demo/default pins. The welding library carries feature-selected wgpu
# dependencies so hosts on adjacent majors do not have to move in lockstep.
# This pin must track welding's default row: the demos take `wgpu.workspace`
# directly AND `welding` with default features, so a mismatch puts two wgpu
# majors in one demo graph.
[workspace.dependencies]
wgpu = "30.0.1"
winit = "0.30.13"
raw-window-handle = "0.6.2"
dpi = "0.1.2"
